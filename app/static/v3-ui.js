(()=>{
const APP_VERSION='3.3.0';
const state={screen:'home',month:new Date().toISOString().slice(0,7),bulkBatch:null};
const q=s=>document.querySelector(s),qa=s=>[...document.querySelectorAll(s)];
const euro=c=>new Intl.NumberFormat('fr-FR',{style:'currency',currency:'EUR',maximumFractionDigits:2}).format((Number(c)||0)/100);
const esc=v=>{const d=document.createElement('div');d.textContent=v??'';return d.innerHTML};
const dateLabel=v=>v?new Intl.DateTimeFormat('fr-FR',{day:'numeric',month:'short'}).format(new Date(`${v}T12:00:00`)):'—';
const api=async(url,opts={})=>{const headers=opts.body instanceof FormData?{}:{'Content-Type':'application/json'};const r=await fetch(url,{cache:'no-store',...opts,headers:{...headers,...(opts.headers||{})}});if(!r.ok)throw new Error((await r.text())||`HTTP ${r.status}`);return r.status===204?null:r.json()};
const screen=name=>q(`[data-screen="${name}"]`);
function skeleton(){return '<div class="skeleton tall"></div>'}
function pageHead(kicker,title,copy=''){return `<div class="page-head"><p class="eyebrow">${esc(kicker)}</p><h1>${esc(title)}</h1>${copy?`<p class="hero-copy" style="color:var(--muted);margin-top:5px">${esc(copy)}</p>`:''}</div>`}
function nav(name){state.screen=name;qa('.screen').forEach(x=>x.classList.toggle('active',x.dataset.screen===name));qa('.bottom-nav [data-nav]').forEach(x=>x.classList.toggle('active',x.dataset.nav===name));window.scrollTo({top:0,behavior:'instant'});render(name)}
function healthLabel(s){return({excellent:'Excellent',good:'Bon',watch:'À surveiller',fragile:'Fragile'})[s]||s}
function safeLabel(s){return({comfortable:'Confortable',prudent:'Vigilance',tight:'Serré',critical:'Critique'})[s]||s||'—'}
function goalStatus(s){return({achieved:'Atteint',on_track:'Dans les temps',at_risk:'À surveiller',off_track:'En retard',late:'Échéance dépassée',no_deadline:'Sans échéance'})[s]||s}
async function renderHome(){const root=screen('home');root.innerHTML=pageHead('Aujourd’hui','Décider sereinement','Ta situation, ce qui arrive et ce que tu peux réellement dépenser.')+skeleton();try{const data=await api('/api/v3/overview'),c=data.cockpit,s=c.safe_to_spend,h=data.health,f=c.forecast;root.innerHTML=pageHead('Aujourd’hui','Décider sereinement','Ta situation, ce qui arrive et ce que tu peux réellement dépenser.')+`<section class="card hero"><div class="hero-top"><div><p class="eyebrow">Disponible sans compromettre le mois</p><span class="status-pill">${safeLabel(s.status)}</span></div><small>${Math.round((c.confidence?.score||0)*100)} % confiance</small></div><div class="hero-amount">${euro(s.until_income_cents)}</div><p class="hero-copy">Budget sécurisé jusqu’au prochain revenu structurant. Aujourd’hui : ${euro(s.today_cents)} · 7 jours : ${euro(s.week_cents)}.</p></section><section class="metric-grid"><article class="card metric"><span>Solde protégé</span><strong>${euro(c.opening_balance_cents)}</strong><small>comptes Safe to Spend</small></article><article class="card metric"><span>Point bas prévu</span><strong>${euro(f.low_point?.balance_cents)}</strong><small>${dateLabel(f.low_point?.date)}</small></article><article class="card metric"><span>Prochaine sortie</span><strong>${c.next_outflow?euro(Math.abs(c.next_outflow.amount_cents)):'—'}</strong><small>${c.next_outflow?`${dateLabel(c.next_outflow.date)} · ${esc(c.next_outflow.label)}`:'aucune détectée'}</small></article><article class="card metric"><span>Prochain revenu</span><strong>${c.next_income?euro(c.next_income.amount_cents):'—'}</strong><small>${c.next_income?`${dateLabel(c.next_income.date)} · ${esc(c.next_income.label)}`:'à confirmer'}</small></article></section><section class="card"><div class="section-head"><div><p class="eyebrow">Financial Health</p><h2>${healthLabel(h.status)}</h2></div><span class="chip active">Méthode V3</span></div><div class="health"><div class="score">${h.score}</div><div><div class="row"><div><strong>Qualité des données</strong><small>35 % du score</small></div><b>${h.components.data_quality}</b></div><div class="row"><div><strong>Liquidité</strong><small>30 % du score</small></div><b>${h.components.liquidity}</b></div><div class="row"><div><strong>Épargne</strong><small>20 % du score</small></div><b>${h.components.savings}</b></div><div class="row"><div><strong>Cash-flow</strong><small>15 % du score</small></div><b>${h.components.cashflow}</b></div></div></div></section>${h.alerts?.length?`<section class="card"><div class="section-head"><div><p class="eyebrow">À traiter</p><h2>Alertes prioritaires</h2></div><span class="chip">${h.alerts.length}</span></div>${h.alerts.map(a=>`<div class="row"><div><strong>${esc(a.title)}</strong><small>${esc(a.detail)}</small></div><span class="${a.severity==='critical'?'danger':a.severity==='warning'?'warning':''}">●</span></div>`).join('')}</section>`:''}<section class="card"><div class="section-head"><div><p class="eyebrow">À venir</p><h2>Calendrier financier</h2></div><button class="chip" data-go="month">Voir le mois</button></div>${(f.events||[]).slice(0,6).map(e=>`<div class="row"><div><strong>${esc(e.label)}</strong><small>${dateLabel(e.date)} · ${esc(e.certainty)}</small></div><div class="money ${e.amount_cents>0?'positive':'negative'}">${e.amount_cents>0?'+':''}${euro(e.amount_cents)}</div></div>`).join('')||'<div class="empty-state">Aucun événement prévu.</div>'}</section><section class="card"><div class="section-head"><div><p class="eyebrow">Journal</p><h2>Activité récente</h2></div><button class="chip" data-go="more">Tout voir</button></div>${(data.activity||[]).slice(0,5).map(a=>`<div class="row"><div><strong>${esc(a.title)}</strong><small>${esc(a.details||a.event_type)} · ${esc(a.created_at)}</small></div></div>`).join('')||'<div class="empty-state">Le journal se remplira avec les actions V3.</div>'}</section>`;bindGo(root)}catch(e){root.innerHTML+=`<div class="card danger">Accueil indisponible : ${esc(e.message)}</div>`}}
async function renderMonth(){const root=screen('month');root.innerHTML=pageHead('Pilotage','Ton mois')+skeleton();try{const d=await api(`/api/v2.1/months/${state.month}`),c=d.current,p=d.previous,a3=d.average_3m,a6=d.average_6m;const m=new Intl.DateTimeFormat('fr-FR',{month:'long',year:'numeric'}).format(new Date(`${state.month}-15T12:00:00`));root.innerHTML=pageHead('Pilotage','Ton mois')+`<section class="card"><div class="section-head"><button class="icon-btn" id="prevMonth">‹</button><h2 style="text-transform:capitalize">${m}</h2><button class="icon-btn" id="nextMonth">›</button></div></section><section class="metric-grid"><article class="card metric"><span>Revenus</span><strong>${euro(c.income_cents)}</strong><small>${euro(c.income_cents-p.income_cents)} vs M-1</small></article><article class="card metric"><span>Fixes</span><strong>${euro(c.fixed_cents)}</strong><small>${euro(c.fixed_cents-p.fixed_cents)} vs M-1</small></article><article class="card metric"><span>Variables</span><strong>${euro(c.variable_cents)}</strong><small>${euro(c.variable_cents-p.variable_cents)} vs M-1</small></article><article class="card metric"><span>Épargne</span><strong>${euro(c.saving_cents)}</strong><small>${euro(c.saving_cents-p.saving_cents)} vs M-1</small></article></section><section class="card"><div class="section-head"><div><p class="eyebrow">Comparaison</p><h2>Rythme de dépenses</h2></div></div><div class="metric-grid"><div class="metric"><span>Mois précédent</span><strong>${euro(p.spent_cents)}</strong></div><div class="metric"><span>Moyenne 3 mois</span><strong>${euro(a3.spent_cents)}</strong></div><div class="metric"><span>Moyenne 6 mois</span><strong>${euro(a6.spent_cents)}</strong></div><div class="metric"><span>Clôture projetée</span><strong>${euro(c.projected_close_cents)}</strong></div></div></section><section class="card"><div class="section-head"><div><p class="eyebrow">Catégories</p><h2>Où part l’argent</h2></div></div>${Object.entries(d.categories||{}).sort((a,b)=>b[1]-a[1]).map(([k,v])=>`<div class="row"><div><strong>${esc(k)}</strong></div><div class="money">${euro(v)}</div></div>`).join('')||'<div class="empty-state">Aucune dépense catégorisée.</div>'}</section>`;q('#prevMonth').onclick=()=>shiftMonth(-1);q('#nextMonth').onclick=()=>shiftMonth(1)}catch(e){root.innerHTML+=`<div class="card danger">Mois indisponible : ${esc(e.message)}</div>`}}
function shiftMonth(delta){const d=new Date(`${state.month}-15T12:00:00`);d.setMonth(d.getMonth()+delta);state.month=d.toISOString().slice(0,7);renderMonth()}
async function renderMovements(){const root=screen('movements');root.innerHTML=pageHead('Transactions','Mouvements & imports','Corriger, apprendre et alimenter Flow sans doublons.')+skeleton();try{const [mov,summary,accounts]=await Promise.all([api('/api/v2/movements?limit=80'),api('/api/v2.4/imports/summary'),api('/api/accounts')]);root.innerHTML=pageHead('Transactions','Mouvements & imports','Corriger, apprendre et alimenter Flow sans doublons.')+`<section class="card"><div class="section-head"><div><p class="eyebrow">Ingestion</p><h2>Importer plusieurs documents</h2></div><span class="chip active">Analyse avant écriture</span></div><form id="bulkForm" class="stack"><select class="field" name="account">${accounts.map(a=>`<option value="${a.id}">${esc(a.name)}</option>`).join('')}</select><input class="field" type="file" name="files" accept=".pdf,.csv" multiple required><button class="btn primary">Analyser le lot</button></form><div id="bulkResult" class="stack" style="margin-top:10px"></div></section><section class="metric-grid"><article class="card metric"><span>Imports</span><strong>${summary.imports.total||0}</strong><small>${summary.imports.imported||0} transactions</small></article><article class="card metric"><span>Doublons évités</span><strong>${summary.imports.duplicates||0}</strong><small>anti-doublon actif</small></article><article class="card metric"><span>À revoir</span><strong>${summary.imports.review||0}</strong><small>inbox qualité</small></article><article class="card metric"><span>Fiches de paie</span><strong>${summary.payrolls||0}</strong><small>${summary.staging_batches||0} lot(s) en staging</small></article></section><section class="card"><div class="section-head"><div><p class="eyebrow">Historique</p><h2>Derniers mouvements</h2></div><button class="chip" id="openSearchMov">Rechercher</button></div>${mov.map(x=>`<div class="row"><div><strong>${esc(x.user_label||x.label)}</strong><small>${dateLabel(x.booking_date)} · ${esc(x.category||'Non catégorisé')} · ${esc(x.account_name||'')}</small></div><div class="money ${x.amount_cents>0?'positive':''}">${x.amount_cents>0?'+':''}${euro(x.amount_cents)}</div></div>`).join('')}</section>`;q('#bulkForm').onsubmit=analyzeBulk;q('#openSearchMov').onclick=openSearch}catch(e){root.innerHTML+=`<div class="card danger">Mouvements indisponibles : ${esc(e.message)}</div>`}}
async function analyzeBulk(ev){ev.preventDefault();const form=ev.currentTarget,files=[...form.elements.files.files],out=q('#bulkResult');if(!files.length)return;const fd=new FormData();fd.append('account_id',form.elements.account.value);files.forEach(f=>fd.append('files',f));out.innerHTML='<div class="skeleton"></div>';try{const d=await api('/api/imports/bulk/analyze',{method:'POST',body:fd});state.bulkBatch=d.batch_id;out.innerHTML=`<div class="row"><div><strong>${d.documents.length} document(s) analysé(s)</strong><small>${d.counts.statement||0} relevé(s) · ${d.counts.payroll||0} paie(s) · ${d.counts.warning||0} alerte(s)</small></div></div>${d.documents.map(x=>`<div class="row"><div><strong>${esc(x.filename)}</strong><small>${esc(x.document_type)} · ${esc(x.period||'période inconnue')} ${x.warning?'· '+esc(x.warning):''}</small></div><span class="chip">${esc(x.status||'staging')}</span></div>`).join('')}<div class="toolbar"><button class="btn secondary" id="discardBulk">Abandonner</button><button class="btn primary" id="commitBulk">Valider le lot</button></div>`;q('#commitBulk').onclick=commitBulk;q('#discardBulk').onclick=discardBulk}catch(e){out.innerHTML=`<div class="danger">Analyse impossible : ${esc(e.message)}</div>`}}
async function commitBulk(){if(!state.bulkBatch)return;const out=q('#bulkResult');try{const d=await api(`/api/imports/bulk/${state.bulkBatch}/commit`,{method:'POST'});await log('import','Lot d’import validé',`${d.transactions||0} transaction(s), ${d.duplicates||0} doublon(s)`,'bulk_import',String(state.bulkBatch));out.innerHTML=`<div class="card"><strong>Import terminé</strong><p class="hero-copy" style="color:var(--muted)">${d.statements||0} relevé(s) · ${d.payrolls||0} paie(s) · ${d.transactions||0} transaction(s) · ${d.duplicates||0} doublon(s)</p></div>`;setTimeout(renderMovements,700)}catch(e){out.innerHTML=`<div class="danger">Validation impossible : ${esc(e.message)}</div>`}}
async function discardBulk(){if(!state.bulkBatch)return;await api(`/api/imports/bulk/${state.bulkBatch}`,{method:'DELETE'});state.bulkBatch=null;q('#bulkResult').innerHTML='<div class="empty-state">Lot abandonné. Aucune donnée importée.</div>'}
async function renderWealth(){const root=screen('wealth');root.innerHTML=pageHead('Patrimoine','Ta valeur nette')+skeleton();try{const d=await api('/api/v2.2/wealth');root.innerHTML=pageHead('Patrimoine','Ta valeur nette','Comptes, épargne, investissements, actifs réels, dettes et objectifs.')+`<section class="card hero"><p class="eyebrow">Patrimoine net</p><div class="hero-amount">${euro(d.net_worth_cents)}</div><p class="hero-copy">${euro(d.total_assets_cents)} d’actifs · ${euro(d.total_debt_cents)} de dettes.</p></section><section class="metric-grid"><article class="card metric"><span>Liquidités</span><strong>${euro(d.cash_cents)}</strong></article><article class="card metric"><span>Épargne</span><strong>${euro(d.savings_cents)}</strong></article><article class="card metric"><span>Investissements</span><strong>${euro(d.investments_cents)}</strong></article><article class="card metric"><span>Actifs réels</span><strong>${euro(d.physical_assets_cents)}</strong></article></section><section class="card"><div class="section-head"><div><p class="eyebrow">Comptes</p><h2>Patrimoine financier</h2></div></div>${(d.accounts||[]).map(a=>`<div class="row"><div><strong>${esc(a.name)}</strong><small>${esc(a.bank||a.kind)} · ${a.balance_as_of||'date inconnue'}</small></div><div class="money">${euro(a.current_balance_cents)}</div></div>`).join('')}</section><section class="card"><div class="section-head"><div><p class="eyebrow">Actifs réels</p><h2>Immobilier & autres</h2></div></div>${(d.assets||[]).map(a=>`<div class="row"><div><strong>${esc(a.name)}</strong><small>${esc(a.asset_type)} · dette ${euro(a.debt_cents)}</small></div><div class="money">${euro(a.value_cents)}</div></div>`).join('')||'<div class="empty-state">Aucun actif réel déclaré.</div>'}</section><section class="card"><div class="section-head"><div><p class="eyebrow">Objectifs</p><h2>Progression</h2></div></div>${(d.goals||[]).map(g=>`<div style="padding:11px 0;border-bottom:1px solid var(--line)"><div class="row" style="border:0;padding:0 0 7px"><div><strong>${esc(g.name)}</strong><small>${goalStatus(g.status)} · cible ${g.target_date||'sans date'}</small></div><div class="money">${Math.round(g.progress_pct||0)} %</div></div><div class="progress"><i style="width:${Math.min(100,g.progress_pct||0)}%"></i></div><small style="display:block;margin-top:5px;color:var(--muted)">${euro(g.effective_current_cents)} / ${euro(g.target_cents)}${g.required_monthly_cents!=null?` · effort requis ${euro(g.required_monthly_cents)}/mois`:''}</small></div>`).join('')||'<div class="empty-state">Aucun objectif.</div>'}</section>`}catch(e){root.innerHTML+=`<div class="card danger">Patrimoine indisponible : ${esc(e.message)}</div>`}}
async function renderMore(){const root=screen('more');root.innerHTML=pageHead('Outils','Plus','Simulations, activité, changelog et maintenance.')+skeleton();try{const [health,activity]=await Promise.all([api('/api/v3/health'),api('/api/v3/activity?limit=20')]);root.innerHTML=pageHead('Outils','Plus','Simulations, activité, changelog et maintenance.')+`<section class="card"><div class="section-head"><div><p class="eyebrow">Décision</p><h2>Simuler un choix</h2></div><span class="chip active">Sans écriture réelle</span></div><form id="simForm" class="stack"><select class="field" name="scenario"><option value="purchase">Achat ponctuel</option><option value="savings_transfer">Versement vers l’épargne</option><option value="monthly_investment">Hausse investissement mensuel</option><option value="future_expense">Dépense future</option><option value="target_closing_balance">Objectif de solde fin de mois</option></select><input class="field" name="amount" inputmode="decimal" placeholder="Montant €" required><input class="field" name="date" type="date"><input class="field" name="label" placeholder="Libellé"><button class="btn primary">Simuler</button></form><div id="simResult" style="margin-top:10px"></div></section><section class="card"><div class="section-head"><div><p class="eyebrow">Financial Health</p><h2>${healthLabel(health.status)} · ${health.score}/100</h2></div></div><p class="hero-copy" style="color:var(--muted)">${esc(health.method)}</p></section><section class="card"><div class="section-head"><div><p class="eyebrow">Activity Log</p><h2>Journal système</h2></div></div>${activity.map(a=>`<div class="row"><div><strong>${esc(a.title)}</strong><small>${esc(a.details||a.event_type)} · ${esc(a.created_at)}</small></div></div>`).join('')||'<div class="empty-state">Aucune activité V3.</div>'}</section><section class="card"><div class="toolbar"><button class="btn secondary" id="openChangelog">Changelog</button><button class="btn primary" id="openSystem">System Center</button></div></section>`;q('#simForm').onsubmit=simulate;q('#openChangelog').onclick=openChangelog;q('#openSystem').onclick=openSystem}catch(e){root.innerHTML+=`<div class="card danger">Outils indisponibles : ${esc(e.message)}</div>`}}
async function simulate(ev){ev.preventDefault();const e=ev.currentTarget.elements,p={scenario:e.scenario.value,amount_cents:Math.round((Number(String(e.amount.value).replace(',','.'))||0)*100),due_date:e.date.value||null,label:e.label.value||null};const out=q('#simResult');out.innerHTML='<div class="skeleton"></div>';try{const d=await api('/api/v2.3/simulate',{method:'POST',body:JSON.stringify(p)});await log('simulation','Simulation financière',`${p.scenario} · ${euro(p.amount_cents)}`,'simulation',p.scenario);out.innerHTML=`<div class="metric-grid"><div class="metric"><span>Verdict</span><strong>${esc(d.verdict_label||d.verdict||'—')}</strong></div><div class="metric"><span>Safe to Spend</span><strong>${euro(d.simulated?.safe_to_spend_cents??d.safe_to_spend_cents)}</strong></div><div class="metric"><span>Point bas</span><strong>${euro(d.simulated?.low_point?.balance_cents??d.low_point_cents)}</strong></div><div class="metric"><span>Clôture</span><strong>${euro(d.simulated?.closing_balance_cents??d.closing_balance_cents)}</strong></div></div><p class="hero-copy" style="color:var(--muted)">${esc(d.summary||d.explanation||'Simulation calculée sans modifier les données réelles.')}</p>`}catch(e){out.innerHTML=`<div class="danger">Simulation impossible : ${esc(e.message)}</div>`}}
function render(name){({home:renderHome,month:renderMonth,movements:renderMovements,wealth:renderWealth,more:renderMore})[name]?.()}
function bindGo(root=document){root.querySelectorAll('[data-go]').forEach(b=>b.onclick=()=>nav(b.dataset.go))}
function openSearch(){const d=q('#searchDialog');d.showModal();setTimeout(()=>q('#globalSearch').focus(),100)}
let searchTimer;async function doSearch(){clearTimeout(searchTimer);const term=q('#globalSearch').value.trim(),out=q('#globalSearchResults');if(term.length<2){out.innerHTML='<div class="empty-state">Saisis au moins 2 caractères.</div>';return}searchTimer=setTimeout(async()=>{out.innerHTML='<div class="skeleton"></div>';try{const d=await api(`/api/v2.4/search?q=${encodeURIComponent(term)}&limit=8`);out.innerHTML=d.results.length?d.results.map(r=>`<button class="row" style="width:100%;border-left:0;border-right:0;border-top:0;background:transparent;text-align:left" data-result-type="${r.type}"><div><strong>${esc(r.title)}</strong><small>${esc(r.subtitle||r.type)}</small></div>${r.amount_cents!=null?`<div class="money">${euro(r.amount_cents)}</div>`:''}</button>`).join(''):'<div class="empty-state">Aucun résultat.</div>';out.querySelectorAll('[data-result-type]').forEach(b=>b.onclick=()=>{q('#searchDialog').close();nav(['movement','import','payroll','planned'].includes(b.dataset.resultType)?'movements':['goal','asset','account'].includes(b.dataset.resultType)?'wealth':'home')})}catch(e){out.innerHTML=`<div class="danger">Recherche impossible : ${esc(e.message)}</div>`}},180)}
async function openSystem(){q('#settingsDialog').showModal();const root=q('#systemCenter');root.innerHTML=skeleton();try{const [sys,upd]=await Promise.all([api('/api/v3/system'),api('/api/update/status')]);root.innerHTML=`<div class="system-grid"><div class="system-item"><span>Version</span><strong>${esc(sys.version)}</strong><small>${esc((sys.commit||'').slice(0,10)||'commit non exposé')}</small></div><div class="system-item"><span>Base de données</span><strong>${(sys.database.size_bytes/1024).toFixed(0)} Ko</strong><small>${esc(sys.database.path)}</small></div><div class="system-item"><span>Mouvements</span><strong>${sys.database.counts.transactions??'—'}</strong></div><div class="system-item"><span>Imports</span><strong>${sys.database.counts.imports??'—'}</strong></div></div><section class="release"><h3>Apparence</h3><div class="chips" style="margin-top:8px"><button class="chip" data-theme="system">Système</button><button class="chip" data-theme="light">Clair</button><button class="chip" data-theme="dark">Sombre</button></div></section><section class="release"><h3>Mise à jour</h3><p class="hero-copy" style="color:var(--muted)">${esc(upd.message||upd.state)} · locale ${esc(upd.local_version||sys.version)}${upd.remote_version?` · distante ${esc(upd.remote_version)}`:''}</p><div class="toolbar"><button class="btn secondary" id="checkUpdate">Vérifier</button>${upd.update_available?'<button class="btn primary" id="runUpdate">Mettre à jour</button>':''}</div></section><section class="release"><h3>Fonctions expérimentales</h3>${sys.feature_flags.map(f=>`<div class="row"><div><strong>${esc(f.description||f.key)}</strong><small>${esc(f.key)}</small></div><input type="checkbox" data-flag="${esc(f.key)}" ${f.enabled?'checked':''}></div>`).join('')}</section><section class="release"><h3>Données & diagnostic</h3>${Object.entries(sys.database.counts).map(([k,v])=>`<div class="row"><div><strong>${esc(k)}</strong></div><b>${v??'—'}</b></div>`).join('')}</section><button class="btn secondary" id="systemChangelog">Voir le changelog</button>`;root.querySelectorAll('[data-theme]').forEach(b=>b.onclick=()=>setTheme(b.dataset.theme));root.querySelectorAll('[data-flag]').forEach(x=>x.onchange=()=>toggleFlag(x.dataset.flag,x.checked));q('#checkUpdate').onclick=checkUpdate;q('#runUpdate')?.addEventListener('click',runUpdate);q('#systemChangelog').onclick=()=>{q('#settingsDialog').close();openChangelog()}}catch(e){root.innerHTML=`<div class="danger">System Center indisponible : ${esc(e.message)}</div>`}}
async function checkUpdate(){await api('/api/update/check',{method:'POST',body:'{}'});setTimeout(openSystem,1800)}
async function runUpdate(){if(!confirm('Mettre Flow à jour maintenant ?'))return;await api('/api/update',{method:'POST',body:JSON.stringify({confirmation:'METTRE A JOUR'})});await log('update','Mise à jour demandée','Déploiement depuis l’interface Flow');setTimeout(()=>location.reload(),6000)}
async function toggleFlag(key,enabled){await api(`/api/v3/feature-flags/${encodeURIComponent(key)}`,{method:'PATCH',body:JSON.stringify({enabled})})}
function setTheme(theme){localStorage.setItem('flow-theme',theme);applyTheme()}
function applyTheme(){const t=localStorage.getItem('flow-theme')||'system';document.documentElement.dataset.theme=t}
async function openChangelog(){q('#changelogDialog').showModal();const root=q('#changelogList');root.innerHTML=skeleton();try{const d=await api('/api/v3/changelog');root.innerHTML=d.releases.map(r=>`<article class="release"><p class="eyebrow">${esc(r.date)} · ${esc(r.version)}</p><h3>${esc(r.title)}</h3><ul>${r.highlights.map(x=>`<li>${esc(x)}</li>`).join('')}</ul></article>`).join('')}catch(e){root.innerHTML=`<div class="danger">Changelog indisponible : ${esc(e.message)}</div>`}}
async function log(event_type,title,details,entity_type=null,entity_id=null){try{await api('/api/v3/activity',{method:'POST',body:JSON.stringify({event_type,title,details,entity_type,entity_id})})}catch{}}
function connectivity(){let banner=q('#offlineBanner');if(!navigator.onLine&&!banner){banner=document.createElement('div');banner.id='offlineBanner';banner.className='offline-banner';banner.textContent='Hors ligne · données réseau indisponibles';document.body.appendChild(banner)}else if(navigator.onLine&&banner)banner.remove()}
async function versionGuard(){try{const d=await api('/api/version');if(d.version!==APP_VERSION){const key=`flow-reload-${d.version}`;if(!sessionStorage.getItem(key)){sessionStorage.setItem(key,'1');location.reload()}}}catch{}}
async function pwa(){if(!('serviceWorker'in navigator))return;try{const reg=await navigator.serviceWorker.register(`/sw.js?v=${APP_VERSION}`);await reg.update();let reloading=false;navigator.serviceWorker.addEventListener('controllerchange',()=>{if(reloading)return;reloading=true;location.reload()})}catch{}}
function boot(){applyTheme();qa('[data-nav]').forEach(b=>b.addEventListener('click',()=>nav(b.dataset.nav)));qa('[data-close]').forEach(b=>b.onclick=()=>q(`#${b.dataset.close}`).close());q('#searchBtn').onclick=openSearch;q('#settingsBtn').onclick=openSystem;q('#globalSearch').oninput=doSearch;window.addEventListener('online',connectivity);window.addEventListener('offline',connectivity);connectivity();renderHome();versionGuard();pwa()}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot);else boot();
})();
